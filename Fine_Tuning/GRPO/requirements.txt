# Core training
unsloth
unsloth_zoo
trl>=0.15.0              # >= 0.15 để hỗ trợ use_vllm trong GRPOConfig
transformers>=4.40.0
accelerate>=0.28.0
bitsandbytes>=0.43.0
peft>=0.10.0

# vLLM — generation engine tối ưu (yêu cầu Linux/WSL2 + CUDA 11.8+)
vllm>=0.4.2

# Dataset
datasets>=2.18.0

# Optional (performance)
xformers
sentencepiece

# Config & utilities
PyYAML>=6.0
